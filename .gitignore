# Dependencies (bun install)
node_modules/

# Rust build artifacts (root workspace target)
/target/

# Build output
dist/
out/
*.tsbuildinfo

# macOS Finder metadata
.DS_Store

# benchmark build artifacts (generated crates, target, reports)
benchmarks/.build/

# series-109 lower.ts modularization byte-identical baseline (local only)
packages/compiler/.lower-baseline.txt

# Materialized backlog — machine-owned local state, rewritten by `pm-playbook pull`.
.pm-playbook/backlog/
